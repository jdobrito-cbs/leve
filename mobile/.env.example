# Servidor do Leve (chaves de parceiro, scan por foto e consulta nutricional).
# O scan deriva {base}/scan-food; a consulta, {base}/food-info; a valida????o de
# chave, {base}/partner-keys/validate. Dom??nio oficial:
EXPO_PUBLIC_LEVE_SERVER_URL=https://levemobile.com.br
# Opcional: URL direta do scan (sobrep??e a derivada do servidor).
# EXPO_PUBLIC_SCAN_URL=
# Opcional: mesmo valor de APP_TOKEN do servidor.
EXPO_PUBLIC_SCAN_TOKEN=
