/** Stub — implementação completa chega com a tela de Perfil (lembretes). */
export async function scheduleDoseReminder(_at: Date): Promise<void> {
  // T14 substitui por agendamento real via expo-notifications.
}
